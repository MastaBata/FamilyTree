# FamilyTree — Полный функционал (после MVP)

Этот документ содержит весь функционал, который мы обсудили, но который не вошёл в MVP. Реализуй его после того, как MVP заработает.

---

## 📦 Фаза 2: Подписки и оплата

### Система подписок

**Тарифы Премиум:**
| Период | Цена | Цена/год |
|--------|------|----------|
| 1 год | $12 | $12 |
| 3 года | $30 | $10 |
| 5 лет | $45 | $9 |
| 10 лет | $70 | $7 |

**Family Pack (2 взрослых):**
| Период | Цена |
|--------|------|
| 1 год | $20 |
| 3 года | $50 |
| 5 лет | $75 |
| 10 лет | $120 |

**Детский аккаунт (500 MB, только просмотр):**
| Период | Цена |
|--------|------|
| 1 год | $6 |
| 3 года | $15 |
| 5 лет | $22 |
| 10 лет | $35 |

**Дополнительная память (в год):**
| Пакет | Цена |
|-------|------|
| +1 GB | $2 |
| +5 GB | $8 |
| +10 GB | $15 |
| +25 GB | $30 |

### Реализация оплаты
- Интеграция со **Stripe** (международные карты)
- Интеграция с **LiqPay** (Украина)
- Страница `/subscription` с выбором тарифа
- Страница успешной оплаты
- Webhook для обработки платежей
- Автоматическое обновление `subscription_status` и `subscription_expires_at` в профиле

### Верификация через оплату
- При успешной оплате → `is_verified = true`
- Галочка верификации возле имени в дереве

### Напоминания об окончании подписки
- За 30 дней — email + уведомление в приложении
- За 7 дней — повторное напоминание
- После окончания — аккаунт переходит в режим "только просмотр"
- Данные НЕ удаляются, просто нельзя редактировать

---

## 🎁 Фаза 3: Скидки и приглашения

### Скидки для ближайших родственников (безлимит)
- Если человек в дереве как родитель/ребёнок/брат/сестра/супруг
- Можно дать скидку: **$10/год** вместо $12
- Генерируется персональный промокод
- Таблица `discount_codes`:
  ```sql
  CREATE TABLE public.discount_codes (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code TEXT UNIQUE NOT NULL,
    type TEXT NOT NULL, -- 'relative', 'friend', 'gift'
    discount_percent INTEGER, -- или фиксированная цена
    fixed_price_cents INTEGER,
    created_by UUID REFERENCES public.profiles(id),
    tree_id UUID REFERENCES public.trees(id),
    person_id UUID REFERENCES public.persons(id), -- для кого создан (если родственник)
    max_uses INTEGER DEFAULT 1,
    times_used INTEGER DEFAULT 0,
    expires_at TIMESTAMPTZ,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMPTZ DEFAULT NOW()
  );
  ```

### Скидки для друзей (лимит 3 на всю жизнь)
- У каждого премиум-пользователя — 3 инвайта
- Цена для друга: **$10/год**
- Поле в `profiles`: `friend_invites_remaining INTEGER DEFAULT 3`

### Подарочные коды
- Покупка пачками со скидкой:
  | Количество | Цена | За код |
  |------------|------|--------|
  | 1 | $12 | $12 |
  | 3 | $30 | $10 |
  | 5 | $45 | $9 |
  | 10 | $80 | $8 |
- Два типа кодов:
  - `GIFT-XXXXX-A` — для взрослых (полный премиум)
  - `GIFT-XXXXX-C` — для детей (детский аккаунт)
- Страница покупки подарочных кодов
- Страница активации кода

### Топ-100 реферальная программа
- Счётчик в профиле: сколько людей купили по твоим инвайтам
- Поле в `profiles`: `referral_count INTEGER DEFAULT 0`
- Раз в год — выгрузка топ-100, отправка постеров (вручную или через сервис печати)

---

## 👤 Фаза 4: Расширенный профиль человека

### Дополнительные поля
```sql
ALTER TABLE public.persons ADD COLUMN:
  - nickname TEXT,                    -- Прозвище
  - education TEXT,                   -- Образование
  - military_service TEXT,            -- Военная служба
  - awards TEXT,                      -- Награды
  - hobbies TEXT,                     -- Хобби
  - interesting_facts TEXT,           -- Интересные факты
  - cause_of_death TEXT,              -- Причина смерти
  - burial_place TEXT,                -- Место захоронения
  - burial_place_coords POINT,
  - external_links JSONB DEFAULT '[]' -- [{type: 'wikipedia', url: '...'}, {type: 'facebook', url: '...'}]
```

### Контакты с приватностью
```sql
CREATE TABLE public.person_contacts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  person_id UUID REFERENCES public.persons(id) ON DELETE CASCADE,
  type TEXT NOT NULL, -- phone, email, address, telegram, etc.
  value TEXT NOT NULL,
  privacy TEXT DEFAULT 'tree_members', -- 'public', 'tree_members', 'close_relatives', 'private'
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Дополнительные папки (разделы)
- Пользователь создаёт папки с любыми названиями
- Внутри: текст + фото + файлы
- Уже есть таблица `custom_sections`, добавить:
```sql
CREATE TABLE public.custom_section_files (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  section_id UUID REFERENCES public.custom_sections(id) ON DELETE CASCADE,
  url TEXT NOT NULL,
  file_type TEXT,
  original_filename TEXT,
  file_size_bytes INTEGER,
  sort_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 🎙️ Фаза 5: Аудио и голосовые послания

### Загрузка аудио
- Поддержка форматов: mp3, m4a, wav, ogg
- Лимит: 10 минут на файл (для обычного премиума)
- Конвертация в AAC 128kbps для экономии места

### Автоматическая транскрипция
- Интеграция с **OpenAI Whisper API**
- После загрузки → отправка на транскрипцию
- Сохранение текста в `documents.transcription`
- Стоимость: ~$0.006 за минуту

### Голосовое послание
- Специальный тип записи: "Послание потомкам"
- Сохраняется как аудио навсегда (не только текст)
- Пометка в БД: `is_voice_message BOOLEAN DEFAULT FALSE`
- Особое отображение в профиле

---

## 🗺️ Фаза 6: Карта родственников

### Глобальная карта
- Библиотека: **Mapbox GL** или **Leaflet** с OpenStreetMap
- Точки на карте — где живут родственники
- Кластеризация при много точках рядом
- Клик на точку → список людей в этом месте

### Настройки приватности местоположения
```sql
ALTER TABLE public.persons ADD COLUMN:
  - location_privacy TEXT DEFAULT 'city' -- 'exact', 'city', 'country', 'hidden'
```
- `exact` — точный адрес виден всем в дереве
- `city` — только город и страна
- `country` — только страна
- `hidden` — не показывать на карте

### Карта миграций
- Линии между местами: рождение → жизнь → смерть
- Анимация по поколениям
- Фильтр по временным периодам

---

## 📅 Фаза 7: Таймлайн семьи

### Хронологическая лента
- Все события семьи на одной ленте:
  - Рождения
  - Свадьбы
  - Разводы
  - Смерти
  - Переезды
- Фильтры по типу события, по ветке семьи

### Наложение на историю
- Параллельная лента исторических событий
- API или статичные данные: войны, революции, важные даты
- "Дедушка родился за год до начала войны"

---

## 🧠 Фаза 8: Умные функции

### Поиск дубликатов
- При добавлении человека — проверка на похожих
- Сравнение по: имя + фамилия + год рождения
- Подсказка: "Возможно, этот человек уже есть в дереве"

### Подсказки связей
- "У Ивана и Марии общий отец — сделать их братом и сестрой?"
- Автоматическое определение братьев/сестёр

### Автоподсчёт родства
- Функция расчёта степени родства между двумя людьми
- "Это твой троюродный дядя"
- Алгоритм поиска общего предка + подсчёт поколений
```typescript
function calculateRelationship(person1Id: string, person2Id: string): string {
  // Найти общего предка
  // Посчитать поколения от каждого до предка
  // Вернуть название родства
}
```

### Поиск по открытым базам
- Интеграция с:
  - **ОБД Мемориал** (obd-memorial.ru) — погибшие в ВОВ
  - **Подвиг народа** (podvignaroda.ru) — награды ВОВ
  - **Память народа** (pamyat-naroda.ru)
- Поиск по ФИО + году рождения
- Отображение результатов внутри приложения
- Возможность привязать найденную запись к человеку в дереве

---

## 🔔 Фаза 9: Уведомления

### Типы уведомлений
```sql
CREATE TABLE public.notifications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES public.profiles(id),
  type TEXT NOT NULL, -- 'new_relative', 'access_request', 'birthday', 'memorial', 'subscription_expiring', 'tree_update'
  title TEXT NOT NULL,
  body TEXT,
  data JSONB, -- дополнительные данные (tree_id, person_id, etc.)
  is_read BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### События для уведомлений
- "Твоя тётя добавила 5 новых родственников"
- "Кто-то запросил доступ к дереву"
- "Сегодня день рождения у [имя]"
- "Годовщина памяти [имя]"
- "Ваша подписка заканчивается через 30 дней"

### Email-уведомления
- Интеграция с **Resend** или **SendGrid**
- Настройки: какие уведомления получать на email

### Push-уведомления (для будущего мобильного приложения)
- Сохранение push-токенов
- Отправка через Firebase Cloud Messaging

---

## 🖨️ Фаза 10: Экспорт и печать

### PDF-постер
- Генерация красивого дерева в PDF
- Библиотека: **@react-pdf/renderer** или **Puppeteer**
- Разные размеры: A4, A3, A2, A1
- Разные стили оформления

### Локальная копия (бэкап)
- Кнопка "Скачать копию дерева"
- Формат: ZIP-архив
  - `tree.json` — все данные
  - `/photos/` — все фото
  - `/documents/` — все документы
- Возможность восстановления из бэкапа

### Заказ печати (будущее)
- Интеграция с сервисом печати (Printful, Gooten)
- Заказ постера с доставкой
- Дополнительный доход

---

## 🔐 Фаза 11: Верификация людей в дереве

### Верификация детей
- Загрузка свидетельства о рождении
- Проверка: автоматическая (Veriff/Jumio) или ручная модерация
- После верификации — галочка у профиля

### Верификация умерших
- Загрузка: паспорт или свидетельство о смерти
- Ручная модерация

### Таблица верификаций
```sql
CREATE TABLE public.verification_requests (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  person_id UUID REFERENCES public.persons(id),
  requested_by UUID REFERENCES public.profiles(id),
  document_url TEXT NOT NULL,
  document_type TEXT, -- 'birth_certificate', 'death_certificate', 'passport'
  status TEXT DEFAULT 'pending', -- 'pending', 'approved', 'rejected'
  reviewed_by UUID REFERENCES public.profiles(id),
  reviewed_at TIMESTAMPTZ,
  rejection_reason TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 👶 Фаза 12: Детские аккаунты

### Особенности детского аккаунта
- 500 MB хранилища
- Только просмотр (без редактирования по умолчанию)
- Привязан к родительскому аккаунту
- Родитель может разрешить редактирование в настройках

### Апгрейд в 18 лет
- При достижении 18 лет — уведомление
- Привязка своей карты → бесплатный апгрейд до полного премиума
- Поле в `profiles`: `birth_date DATE` для проверки возраста

### Управление детскими аккаунтами
- Страница в настройках родителя
- Список привязанных детских аккаунтов
- Переключатель "Разрешить редактирование"

---

## 🔄 Фаза 13: Realtime и коллаборация

### Realtime обновления
- Supabase Realtime subscriptions
- Когда кто-то добавляет человека → все видят сразу
- Когда кто-то редактирует → обновление в реальном времени
- Индикатор "Сейчас редактирует: Мария"

### История изменений
```sql
CREATE TABLE public.audit_log (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tree_id UUID REFERENCES public.trees(id),
  user_id UUID REFERENCES public.profiles(id),
  action TEXT NOT NULL, -- 'create', 'update', 'delete'
  entity_type TEXT NOT NULL, -- 'person', 'relation', 'photo', etc.
  entity_id UUID,
  old_data JSONB,
  new_data JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```
- Возможность откатить изменения
- "Кто и когда изменил"

---

## 📱 Фаза 14: Мобильное приложение

### Варианты реализации
1. **React Native** — переиспользование логики с веба
2. **Swift (iOS) + Kotlin (Android)** — нативная производительность
3. **PWA** — Progressive Web App как промежуточный вариант

### Особенности мобильной версии
- Упрощённая визуализация дерева
- Быстрое добавление фото с камеры
- Push-уведомления
- Офлайн-режим (просмотр закэшированных данных)

---

## 🌍 Фаза 15: Интернационализация

### Мультиязычность
- Библиотека: **next-intl** или **i18next**
- Языки: русский, украинский, английский (для начала)
- Автоопределение языка по браузеру
- Переключатель в настройках

### Локализация дат и имён
- Разные форматы дат для разных регионов
- Поддержка отчеств (для славянских стран)
- Поддержка разных систем имён

---

## 🔮 Фаза 16: Будущие фичи

### Капсула времени
- Сообщение, которое откроется через N лет
- Или откроется конкретному человеку когда ему исполнится 18
```sql
CREATE TABLE public.time_capsules (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tree_id UUID REFERENCES public.trees(id),
  created_by UUID REFERENCES public.profiles(id),
  recipient_person_id UUID REFERENCES public.persons(id),
  content TEXT,
  attachments JSONB, -- [{type: 'photo', url: '...'}, ...]
  unlock_type TEXT, -- 'date', 'age', 'event'
  unlock_date TIMESTAMPTZ,
  unlock_age INTEGER,
  is_unlocked BOOLEAN DEFAULT FALSE,
  unlocked_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Импорт из GEDCOM
- Стандартный формат генеалогических данных
- Парсер GEDCOM → импорт в нашу структуру
- Библиотека: `gedcom-parser` или свой парсер

### AI-помощник
- Расшифровка старых документов (рукописный текст)
- Интеграция с Claude API
- "Загрузи фото документа — AI расшифрует текст"

### ДНК-интеграция
- Импорт результатов ДНК-тестов (23andMe, AncestryDNA)
- Показ этнического состава
- Поиск генетических родственников

---

## Порядок реализации (рекомендуемый)

1. **Фаза 2** — Подписки и оплата (без денег проект не выживет)
2. **Фаза 3** — Скидки и приглашения (рост пользователей)
3. **Фаза 4** — Расширенный профиль (больше ценности)
4. **Фаза 9** — Уведомления (вовлечённость)
5. **Фаза 5** — Аудио и голосовые
6. **Фаза 8** — Умные функции
7. **Фаза 6** — Карта
8. **Фаза 7** — Таймлайн
9. **Фаза 10** — Экспорт
10. **Фаза 11** — Верификация людей
11. **Фаза 12** — Детские аккаунты
12. **Фаза 13** — Realtime
13. **Фаза 14** — Мобильное приложение
14. **Фаза 15** — Интернационализация
15. **Фаза 16** — Будущие фичи

---

Удачи с разработкой! 🌳
